namespace soru9;

class Program
{
    static void Main(string[] args)
    {
        /*Kullanıcıdan alınan iki sayı arasındaki küçük olanı bulan bir program yazın.
       
        Console.WriteLine("İlk sayı değerini girin: ");
        byte values=byte.Parse(Console.ReadLine());
        Console.WriteLine("2.sayı değerini girin: ");
        byte values2=byte.Parse(Console.ReadLine());

        string resultMessage= values<values2
                              ?$"{values} küçüktür."
                              :$"{values2} küçüktür.";
        Console.WriteLine(resultMessage);
        */

    }
}
