namespace soru5;

class Program
{
    static void Main(string[] args)
    {
        /*Kullanıcıdan alınan iki sayı için üs alma işlemi yapan bir program yazın (örneğin, 2^3).
        
        Console.WriteLine("İlk sayıyı giriniz: ");
        double values=double.Parse(Console.ReadLine());
        Console.WriteLine("İkinci sayıyı giriniz: ");
        double values2=double.Parse(Console.ReadLine());

        double newValues=Math.Pow(values,values2);
        Console.WriteLine(newValues);
        */

    }
}
